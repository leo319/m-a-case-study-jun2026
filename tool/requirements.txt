# Milestone 1 (the spine) deliberately keeps dependencies tiny.
# Everything else uses the Python 3.11+ standard library (urllib, hashlib, json, ...).
jsonschema>=4.21
PyYAML>=6.0
tzdata>=2024.1  # IANA tz database for Pacific-time run folders (Windows ships none)
